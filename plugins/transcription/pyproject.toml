[project]
name = "transcription-mcp"
version = "0.4.0"
description = "Local-first audio transcription MCP server — powered by scribe CLI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.26.0",
]
